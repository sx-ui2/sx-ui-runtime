# sx-ui Runtime Assets

This repository publishes managed runtime binaries used by `sx-ui`.

Published releases:
- `sing-box-stats-v*`
- `caddy-naive-v*`

Included workflows build Linux `amd64` and `arm64` runtime archives for:
- sing-box with stats support
- Caddy naive runtime with sx-ui traffic metrics patch
